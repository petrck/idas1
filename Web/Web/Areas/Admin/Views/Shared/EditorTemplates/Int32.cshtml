@using Kendo.Mvc.UI
@model int?

@(Html.Kendo().IntegerTextBoxFor(m => m)
    //.EditorAttributes(new { style = "width:100%" })
    .Min(int.MinValue).Max(int.MaxValue).Enable(!ViewData.ModelMetadata.IsReadOnly)
    )